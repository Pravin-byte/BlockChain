{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "scripts": {
  "postinstall": "cd ../client && npm install && npm run build && cp -r dist/* ../server/public/"
}

  },
  "author": "",
  "license": "ISC",
  "description": "Blockchain demo server with REST API endpoints",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "path-to-regexp": "^6.2.1",
    "uuid": "^11.1.0"
  },
  "keywords": [
    "blockchain",
    "nodejs",
    "express",
    "api",
    "demo"
  ]
}
